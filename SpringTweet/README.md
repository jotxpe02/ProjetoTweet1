# SpringTweet - projeto pronto para o Trabalho NT2

Estrutura: controllers originais foram mantidos; adicionadas camadas Service, config OpenAPI e frontend estático em src/main/resources/static/index.html

Para rodar:

```bash
mvn spring-boot:run
```

Swagger UI disponível em http://localhost:8080/swagger-ui.html
